STABLE RECOVERY BUILD

This version restores the last stable HTML/CSS/JS architecture before the all-WebP conversion.

Upload/replace:
- index.html
- styles.css
- script.js
- merge the entire assets folder into the repository assets folder

Important:
- Dessert assets use PNG/JPG again.
- Only the three intro backgrounds use WebP.
- Corrected Napoleon PNGs have real transparency.
- Old dessert .webp files may remain in the repository; this code does not reference them.
- Foreground files are not used and are not included.
